# ══════════════════════════════════════════════════════════════
#  ZigZag V32 — Variables de entorno CORREGIDAS
#  Pega estas variables en Railway → Variables
#  TODOS los valores sin comillas en Railway (Railway las añade)
# ══════════════════════════════════════════════════════════════

# ── BingX API ──────────────────────────────────────────────────
BINGX_API_KEY=tu_api_key_aqui
BINGX_SECRET_KEY=tu_secret_key_aqui

# ── Telegram ───────────────────────────────────────────────────
TELEGRAM_TOKEN=tu_token_aqui
TELEGRAM_CHAT_ID=tu_chat_id_aqui

# ── Timeframe ──────────────────────────────────────────────────
TIMEFRAME=3m
KLINE_LIMIT=120
CANDLE_SLEEP=60

# ── ZigZag Canal ───────────────────────────────────────────────
PIVOT_LEN=5
ATR_LEN=14

# ── Overshoot en pips (dinámico según precio del par) ──────────
# BTC pip=1.0  → SHORT_PIPS=45 = $45 encima del canal
# ETH pip=0.1  → SHORT_PIPS=45 = $4.5 encima del canal
# XRP pip=0.001→ SHORT_PIPS=45 = $0.045 encima del canal
SHORT_PIPS=45
LONG_PIPS=30

# ── SL/TP ──────────────────────────────────────────────────────
SL_ATR_MULT=1.5
MIN_RR=1.2

# ── Filtros — RELAJADOS para generar señales ───────────────────
# ADX: bajado de 25 a 20 (más señales sin perder calidad)
ADX_MIN=20
ADX_LEN=14

# EMAs
EMA_FAST=7
EMA_MED=17
EMA_SLOW=21

# Volumen: VOL_MULT=1.0 con VOL_FILTER=true = solo requiere
# que el volumen actual >= media20 (umbral mínimo razonable)
VOL_FILTER=true
VOL_MULT=1.0

# ── Gestión de riesgo ──────────────────────────────────────────
LEVERAGE=10
RISK_PCT=1.5
MAX_POSITIONS=3
MAX_DAILY_LOSS=5.0

# ── Scanner ────────────────────────────────────────────────────
# WHITELIST_ONLY=true usa los 12 pares hardcodeados en config.py
# MIN_QUOTE_VOL: bajado para que pasen más pares de la whitelist
WHITELIST_ONLY=true
TOP_PAIRS=12
MIN_QUOTE_VOL=10000000
MIN_PRICE_USDT=0.001

# ── Sistema ────────────────────────────────────────────────────
PORT=8080
